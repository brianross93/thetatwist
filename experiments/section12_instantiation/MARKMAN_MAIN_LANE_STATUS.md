# Markman main-lane status

## Objective

Test the corrected Section 12 construction at $d=3$ against the weaker
semiregularity criterion.  The six protected mixed classes are

$$
C_{12}-3A_{12},\ C_{13}-3A_{13},\ C_{14}-3A_{14},\
C_{23}-3A_{23},\ C_{24}-3A_{24},\ C_{34}-3A_{34}.
$$

The goal is to construct the corrected object and determine whether all six
classes lie in the kernel of its Atiyah evaluation map.

This is a test of Markman's $d=3$ construction.  It is not a proof of the
Hodge conjecture.

## What is now exact

1. The Section 12 twist is $3\Theta$ in the corrected cyclic presentation.
   The correction is the unique integral twist that places the Chern
   character in the stated secant plane.
2. The independent $\mathbf F_{31}$ fibre has six distinct surface sectors.
   It is not a cyclic transport of one seed.  All six length-two schemes,
   all 486 $K$-orbit lifts, and all six local Hartshorne--Serre orientations
   are explicit.
3. On each surface, the pair

   $$
   V_0=\mathcal O_S(3H)\oplus\mathcal O_S(-H),
   \qquad
   0\to\mathcal O_S(3H)\to V_1\to I_W(-H)\to0
   $$

   has equal determinant and
   $c_2(V_1)-c_2(V_0)=2[p]$.  Six copies give the required
   $12[\mathrm{pt}]$ correction.
4. The bidirectional local complex has shape $6\to16\to9$.  All 16 ordered
   first-jet products vanish over the polynomial ring.  Therefore all six
   local mixed cups vanish exactly.
5. The adjacent conductor restrictions split after orienting the extension
   by $\delta_s/(x_sy_s^2)$.
6. Local duality gives

   $$
   \mathrm{Ext}^1_S(I_W,\mathcal O_S(4H))
   \cong
   \mathrm{Ext}^2_S(\mathcal O_W,\mathcal O_S(4H)).
   $$

   Thus the six local orientations determine the six abstract global
   Hartshorne--Serre classes.  A full 1,135-generator presentation of
   $I_W$ is not required for this step.
7. For every surface, the exact degree-two value-and-tangent matrix has
   shape $162\times1296$ and rank 161.  Two normalization jets raise the
   rank to 163.  Twelve explicit global sections $X_s,Q_s$ are emitted.
8. The translated generators span both conormal directions at all 81
   support lifts on every surface.  By Nakayama, their ideal and $I_W$ have
   the same stalk at every support point.
9. The higher theta ring now includes levels $5,6,8,9,10$, all 81
   $K$-isotypic bases at each level, and the four required multiplication
   programs.
10. The exact rational $K$ action is known.  The original 16-chart product
    cover is not $K$-stable.  Its orbit has 1,296 charts.  The repository
    records 1,024 generator-refined intersections and 2,048 translated edge
    pullbacks.
11. The non-split Hartshorne--Serre coefficient space is now exact before
    conductor matching.  Its six surface dimensions are

    ```text
    401, 400, 401, 400, 401, 401,
    ```

    for a total of 2,404.  The extension compatibility removes twenty
    coefficients from the 2,424-dimensional split comparator.
12. Evaluation at the twelve selected surface-normal supports gives an
    exact $48\times24$ matrix of rank 24.  Per normal direction, its image is
    generated by the scalar diagonal $E_{00}+E_{11}$ and the positive
    off-diagonal $E_{01}$ direction.
13. The scalar product-cover complex

    $$
    \mathbf F_{31}^{16}\longrightarrow
    \mathbf F_{31}^{32}\longrightarrow
    \mathbf F_{31}^{24}
    $$

    has ranks 15 and 17.  All fifteen scalar edge cocycles have explicit
    vertex primitives.  The remaining obstruction is sheaf-valued, not a
    first-cohomology class of the bare cover nerve.
14. For every surface, the degree-six quotient is exact:

    $$
    H^0(5H)^{\oplus2}\longrightarrow H^0(6H)
    \longrightarrow H^0(\mathcal O_{Z_s}(6H))
    $$

    has quotient dimension 302, and value--tangent restriction to $W_s$
    has a 300-dimensional kernel $H^0(I_{W_s}(6H))$.  All 1,800 quotient
    columns, their ambient representatives, their $J_{3,5}$ lift
    coefficients at 486 supports, and the exact all-orders formal division
    formula are emitted.
15. The apparent 104-dimensional $Q\to U$ selector obstruction is now
    separated correctly by the outer Cech differential.  Internally it
    injects into the 462-dimensional $U$ Hom space, but the full outer
    complex splits as

    $$
    [X\to P\to R]\oplus[Q\xrightarrow{\ \pm I\ }E(Q)],
    \qquad \dim R=462-240=222.
    $$

    All 104 selector directions lie in the contractible second summand.
    The surviving point condition is $B_RA=0$, not an additional choice of
    the affine selector matrix $M_Q$.
16. On `Z_2/C0010`, the full 162-generator translated ideal equals the exact
    Hermite ideal.  Degree 11 contains 293 of 297 border generators; a fixed
    degree-12 prefix contains the remaining four.  The equality uses two
    containments and does not use Hermite-kernel saturation.
17. The same chart now has an exact Hartshorne--Serre cochain on a
    graph-Koszul/Shamash carrier of ranks $(1,687,2750,9610)$.  Its six-term
    polynomial-ring boundary identity closes over the surface ring and gives
    the rank-two extension module $0\to S\to V'_U\to I_W\to0$.
18. The full six-surface cubical descent registry contains 486 cells and
    1,296 incidence restrictions.  All degree-zero maps are emitted in
    support-jet coordinates, and all 384 stored standard-basis edge maps
    pass their Hermite factorization.
19. The higher resolution maps $J_0$ through $J_3$ are exact on all 192
    edges and 11,665 retained factors. The 144 faces, 48 cubes, and six
    four-cells close in the retained window, and the relative
    $\mathcal O(4H)$ counit closes across the complete cubical atlas: every
    edge and face closes directly, and all cube/four-cell terms are forced zero
    by the Ext$^2$ carrier typing.
20. The four standard $K$-generator tree comparisons are now exact on their
    complete finite overlap algebras. Their support counts, algebra lengths,
    and rectangular map shapes are

    | generator | supports / length | $J_1$ | $J_2/L_1$ | factorized $J_3$ | $L_0$ |
    |---|---:|---:|---:|---:|---:|
    | $e_0$ | $35/70$ | $491\times687$ | $1966\times2750$ | $6866\times9610$ | $492\times688$ |
    | $e_1$ | $31/62$ | $435\times687$ | $1742\times2750$ | $6082\times9610$ | $436\times688$ |
    | $e_2$ | $35/70$ | $491\times687$ | $1966\times2750$ | $6866\times9610$ | $492\times688$ |
    | $e_3$ | $32/64$ | $449\times687$ | $1798\times2750$ | $6278\times9610$ | $450\times688$ |

    This is 133 support factors and 1,729 verified factorized
    $D_3/J_3$ block identities in total. One orientation-coboundary law,

    $$
    \rho_{K,s}=A_K\frac{u_s}{u_t}J_{8,s},
    \qquad
    q=\frac{\rho}{\det B},
    \qquad
    n=\frac{q}{w_H},
    $$

    produces all four normalizations. The $e_0$ normalization is the constant
    pair $(25,0)$; the other three are nonconstant overlap units. These are
    finite-algebra identities, not four fitted repair rules.
21. A deterministic adapter now binds the four generator artifacts to the
    full typed registry: 324 $K$ edges, 108 order-three cycles, and 486
    commutation squares. It emits support identities and composition
    templates only. It does not yet rebase the rectangular maps into common
    bases, multiply the edge maps around a cell, certify a numeric cell
    closure, or emit a chain homotopy. The strict Laurent/surface-ring lift is
    also still open. No absent map is represented by a zero placeholder.

## Current frontier

The surface and curve part of the successor is now a closed exact
calculation.

The conductor equalizer is the injective map

$$
K_{\mathrm{curve}}:\mathbf F_{31}^{1852}
\hookrightarrow\mathbf F_{31}^{2404}.
$$

Its columns split into 1,824 upper $E_{01}$ columns and 28 compatible
diagonal columns.  There is no projection in the reverse direction.

Every one of the 486 completed supports now has an exact Hensel embedding in

$$
J_{3,5}=\mathbf F_{31}[x,y]/(x^3,y^5).
$$

The cache includes all 11,664 invariant derivative jets $D_i s_j$, all
twelve $194\times625$ surface quotient maps, and certified right inverses.
All 972 quotient evaluations factor through the surface quotients exactly.

For every compatible diagonal pair, the exact divisibility relation is

$$
\alpha-\gamma=xu+y^2v.
$$

It gives the lci lift

$$
P=
\begin{pmatrix}
\alpha-xu&-y^2u\\
-xv&\gamma+xu
\end{pmatrix},
$$

and the two split-frame representatives satisfy

$$
G^{-1}F_{D(x)}G=F_{D(y)}.
$$

All 1,852 coefficient-indexed lifts therefore have zero Hom jump and zero
oriented $P_{1,2}$ residue on the surface/open-formal seam.  The emitted
surface numerator is the exact sparse-zero matrix of shape
$56{,}376\times1{,}852$.  This is a result about the typed surface seam, not
an assertion that the final point-stratum numerator is zero.

The separate local Atiyah calculation is not zero.  The 1,824 upper columns
vanish structurally.  Of the 28 diagonal columns, eight are nonzero.  The
stacked $dx$ block has rank 4, the stacked $dy$ block has rank 4, and their
combined rank is 4.  Thus the surface/curve layer supplies four independent
Atiyah directions.

The contraction of this curve layer with the six ambient bivectors is now
explicit.  All 486 surface coframes are invertible, and 18,144 directional
pieces recompose to the emitted $dx/dy$ operators exactly.  In the order

$$
C_{12},C_{13},C_{14},C_{23},C_{24},C_{34},
$$

the six curve-shadow ranks are

$$
4,3,3,3,3,1,
$$

while their direct sum still has rank 4 and is supported on the same eight
equalizer columns.  These are the $C_{ij}$ principal-part shadows.  They are
not yet the protected $C_{ij}-3A_{ij}$ classes: the scalar $-3A_{ij}$ term
lives in global Cech degree two, and the point-stratum attachment must be
imposed before the curve shadow is a cochain of the coupled object.

The point layer is now typed as

$$
D_{\mathrm{point}}=D_{\mathrm{point,raw}}K_{\mathrm{curve}}.
$$

The branch-labelled precursor has shape $720\times1852$, rank 642, and
kernel dimension 1,210. It is not itself $D_{\mathrm{point}}$. The $Q$
normal-jet restrictions and the outer $Q\to U$ contraction are exact. The
boundary-adapted global cochain calculation now proves that the surviving
222-dimensional adjacent-overlap block satisfies

$$
B_RA=0
$$

on the curve equalizer. Thus the contracted $Q\to U$ sector adds no point
rows there. The normalized point matrix consists of the 144 retained-$R25$
rows and has rank 67:

$$
D_{R25}:\mathbf F_{31}^{1852}\longrightarrow\mathbf F_{31}^{144}.
$$

Its canonical kernel has dimension 1,785. Composing with the curve
equalizer gives the exact global candidate matrix

$$
G=K_{\mathrm{curve}}K_{\mathrm{point}}
\in\operatorname{Mat}_{2404\times1785}(\mathbf F_{31}).
$$

The zero- and first-order abstract $Q$ geometry are no longer missing.  Each of the six
four-theta strata has an exact reduced coordinate algebra

$$
A_Q\cong\mathbf F_{31}[z]/(m_Q),
\qquad \deg m_Q=24,
$$

with square-free $m_Q$.  Both omitted theta sections are units, all defining
operators vanish, and the two line-frame descriptions agree exactly.  The
doubled quotient is now also exact:

$$
B_Q=\mathcal O_X/I_Q^2,
\qquad \dim B_Q=120,
$$

and its 96-dimensional projection kernel is the direct sum of the four
certified conormal copies $A_Qe_j$.  Six Bezout identities
$a_Qm_Q+b_Qm'_Q=1$ prove $\Omega_{A_Q/\mathbf F_{31}}=0$.  Hence the
conormal map

$$
I_Q/I_Q^2\longrightarrow\Omega_X|_Q
$$

is an abstract $A_Q$-linear isomorphism on every $Q$.

This permits an exact conormal-normalized frame.  The repository emits all
24 normalized $24\times4$ blocks and six $96\times96$ identity maps.  Rank,
kernel, exactness, and simultaneous vanishing in the full six-dimensional
$\Lambda^2$ space are invariant under this frame change when it is propagated
through the complete local complex.  The blocks are not the uncomputed
entries of the original invariant-frame Jacobian $J_Q=(D_as_j)$, and they do
not create attachment maps.

The support and conormal parts of the point input are now exact. Every
retained-point support $Q$ is the same four-theta closed scheme as its
three-surface support $U$, every $W_s$ is disjoint from every $Q$, and the
split $Q\to U$ maps preserve all 16 ordered branch channels.

On each $Q$, the first-neighborhood chain-lift space has a 104-dimensional
affine ambiguity:

$$
S_Q=S_0+K M_Q,
\qquad M_Q\in\operatorname{Mat}_{104\times16}(A_Q).
$$

The exact three-surface audit shows that these 104 directions inject into the
*internal* $U$ Hom complex.  The outer Cech differential changes the answer:
the $240$-dimensional image $E(Q)$ pairs with $Q$ by a signed identity and is
contractible.  All 104 selector directions project to zero in
$R=\operatorname{Coeff}(U)/E(\operatorname{Coeff}(Q))$.  Thus no coherent
choice of $M_Q$ remains as an independent obstruction.

The degree-six source for the global comparison is also explicit. It gives
six exact 300-column right-hand sides, $J_{3,5}$ coefficients of their formal
lifts at all 486 supports, and a deterministic all-orders division formula.
The apparent $\mathcal O(10H)$ solve factors through the canonical
trace-zero class

$$
e_s=\delta_s e'_s,
\qquad
e'_s\in\operatorname{Ext}^1(I_{W_s},\mathcal O_S(2H)).
$$

For $b\in H^0(I_{W_s}(6H))$, the reduced product lies in
$H^1(\mathcal O_S(8H))=0$. Its 590-dimensional degree-zero gauge supplies a
primitive $w_b$, and $u_b=\delta_s w_b$ vanishes on the full conductor. The
finite computation reproduces every stored punctured coordinate in all
145,800 support/section instances. The incidence audit checks the 12 named
$T\to U$ arrows on both incident surfaces. This is the exact input that
turns the old conditional outer-Cech lemma into the proved equation
$B_RA=0$.

The boundary-adapted computation is a degree-six probe of the extension. It
does not provide the strict extension action itself. The surface-first
derived model is

$$
J_s=\operatorname{Fib}(M_s\to P_s),\qquad
e_s=(h_s,\omega_s):J_s\to L_s[1],
$$

with one universal law

$$
dh_s=-\omega_s\circ r_s.
$$

The abstract class and completed-local component $\omega_s$ are fixed on all
six surfaces. One chart-local strict representative and its supportwise edge
transport are emitted, all 144 face equations close strictly, and the cube
and four-cell relations vanish by exact carrier degree. This is still a
supportwise Hermite statement, not a whole-overlap-ring representative, so no
common global dg representative $h_s$ or derived $q_1$ evaluation is claimed.
The four frozen local coefficient word
families have exact finite two-open Čech windows, and all 1,944 Laurent lifts
project back to the prior principal-part matrices. The common multiplicative
open/formal cross-Hom source remains the missing object; no absent matrix is
treated as zero. Once it is emitted, derived global sections and tautological
evaluation produce all 300 $q_1$ columns per surface as outputs. Thus the
300 columns and the 192 base-chart edges are not independent repair rules.

On the representative chart `Z_2/C0010`, the actual Hermite ideal now has the
exact first presentation

$$
S^{1887}\longrightarrow S^{170}\longrightarrow I_W\longrightarrow0.
$$

This supplies finite terms for the correction object on one chart.  It does
not truncate away the higher syzygies.  Those syzygies are now respected by
an exact graph-Koszul/Shamash carrier of ranks $(1,687,2750,9610)$.  Its
factored canonical row obeys

$$
\lambda d_3=\sum_{j=0}^{5}(-1)^j f_j\mu_j
$$

in the polynomial ring and is therefore closed over the surface ring.  It
gives the chart-local class
$\operatorname{Ext}^2_S(A_W,S)\cong\operatorname{Ext}^1_S(I_W,S)$ and the
exact rank-two module $0\to S\to V'_U\to I_W\to0$.

The six-surface atlas now has one hash-bound functorial registry: 96 charts,
192 overlaps, 486 cubical cells, 1,296 incidence restrictions, and all 384
stored standard-basis edge maps.  This closes the degree-zero restriction
layer.  The pairwise higher resolution layer is also exact: all 192 atlas
edges carry materialized supportwise Hermite-quotient maps
$J_0,J_1,J_2,J_3$ over their retained Laurent factors.  Across 11,665 such
factors, 81,655 degree-two and 151,645 degree-three component identities
close, including explicit nonzero $\Gamma$ and $\Omega$ blocks.  This does
not alone prove coherent descent.  The next layer is now computed on all 144
faces: every curve sector is strict, and the two theta sectors have nonzero
but exactly null-homotopic defects precisely for axis pairs contained in
$\{0,1,2\}$.  Faces containing axis 3 are strict.  The cube layer has now
closed on all 48 surface cubes: nonzero
$K_4/K_5$ homotopies occur exactly for the 12 cubes with axes $(0,1,2)$,
and the other 36 cubes are strict.  The six four-cells retain the mandatory
$A_2^2$ term and close on all six surfaces.  Every $A_2^2$ term is checked and typed zero;
the nonzero theta contributions $A_1A_3$ and $A_3A_1$ cancel exactly, so
$A_4=0$.  This completes the supportwise cubical coherence of the resolution
carrier in the displayed degree window.

The relative counit now transports across all 192 edges as well.  The
orientation-normalized row has the exact $\mathcal O(4H)$ factor $h^4$.
At all 11,665 retained factors, one system simultaneously enforces all 28
$K_2$ and six equation columns; its rank is 12, its nullity is 4, and its
residual is zero.  This gives 793,220 checked scalar zeros. All 144 face
equations also close strictly on 8,238 supports, with zero edge-gauge defect
and zero canonical face row. All 15,510 cube and 1,824 four-cell equations
then close by exact type, without $\Phi_3$ or $\Phi_4$.

The representative whole-ring realization is now complete on
`Z_2/C0010`. Forty-nine support opens and 170 split-complement opens give a
219-open rank-two frame atlas. The support cover follows from the exact
49-factor dual-number decomposition of $S/I_W$, and the complement cover is
$D(I_W)=\bigcup_aD(g_a)$ for the 170 corner generators. All 23,871
unordered transition circuits use the single formula

$$
G_{x\leftarrow y}=\pi_x\iota_y
=\frac{P^{\rm num}_xJ^{\rm num}_y}{d_xe_y}.
$$

The 160 active complement frames and all 12,720 active complement inverse
pairs replay exactly; the support frames bind actual $d_3$ kernel minors.
Thus this chart now carries a finite strict rank-two model of the
Hartshorne--Serre module. The four standard $K$ generators now carry
independently normalized rectangular comparisons on their complete finite
overlap algebras:

| generator | supports / length | $J_1$ | $J_2/L_1$ | factorized $J_3$ | $L_0$ |
|---|---:|---:|---:|---:|---:|
| $e_0$ | $35/70$ | $491\times687$ | $1966\times2750$ | $6866\times9610$ | $492\times688$ |
| $e_1$ | $31/62$ | $435\times687$ | $1742\times2750$ | $6082\times9610$ | $436\times688$ |
| $e_2$ | $35/70$ | $491\times687$ | $1966\times2750$ | $6866\times9610$ | $492\times688$ |
| $e_3$ | $32/64$ | $449\times687$ | $1798\times2750$ | $6278\times9610$ | $450\times688$ |

The four artifacts contain 133 support factors and close 1,729 factorized
$D_3/J_3$ block identities. Their normalizations are instances of one
orientation-coboundary law,

$$
\rho_{K,s}=A_K\frac{u_s}{u_t}J_{8,s},
\qquad
q=\frac{\rho}{\det B},
\qquad
n=\frac{q}{w_H}.
$$

The $e_0$ normalization is the constant pair $(25,0)$; $e_1,e_2,e_3$ use
nonconstant overlap units. The separated $p/q$ rows replay on their complete
source rows, including the $q$ columns. This is an exact finite-overlap
calculation, not a fitted repair and not a sample.

The typed coherence adapter now contains all 324 edge records, 108
order-three cycle records, and 486 commutation-square records. This closes
the support-identity and composition-template registry only. The rectangular
maps have not yet been rebased into common source and target bases, so the
adapter has not multiplied numeric edge maps around any cell and has not
certified strict cell closure or emitted an explicit chain homotopy.

The strict Laurent/surface-ring lift of the finite counit rows also remains
open, as do the later target-frame/formal restriction and numeric cross-RHom
endpoint. See
`HS_RANK2_FULL_CHART_ATLAS.md`,
`HS_RANK2_K_CHAIN_COMPARISON.md`, `HS_RANK2_K_GENERATOR_OVERLAP_J1.md`, and
`HS_RANK2_K_GENERATOR_SHAMASH.md`, together with
the four generator artifact directories and
`results/section12_instantiation/cm_bridge_hs_rank2_k_coherence_adapter/`.

The earlier supportwise coherence evidence remains documented in
`HS_SHAMASH_OVERLAP_LIFTS.md` and `HS_SHAMASH_SQUARE_COHERENCE.md`.
The cube certificate and reconstruction data are documented in
`HS_SHAMASH_CUBE_COHERENCE.md`.
The final four-cell relation and edgewise counit transport are documented in
`HS_SHAMASH_FOUR_CELL_COHERENCE.md` and
`HS_LAMBDA_OVERLAP_TRANSPORT_FAST.md`.
The all-face counit calculation is documented in
`HS_LAMBDA_FACE_COHERENCE.md`.
The last typed counit relations are documented in
`HS_LAMBDA_CUBE_COHERENCE.md` and `HS_LAMBDA_FOUR_CELL_COHERENCE.md`.

After the strict chart models are compared coherently on their overlaps, the
remaining protected-sector factor is the coefficient-indexed,
residue-projected Yoneda numerator

$$
Q_{\mathrm{pre}}:\mathbf F_{31}^{2404}\longrightarrow\mathbf F_{31}^{6}.
$$

The boundary-adapted $q_1$ primitive does not determine this map: it has no
protected-witness axis and is defined only up to the regular
$H^0(\mathcal O_S(8H))$ gauge. The exact final product is now

$$
T=Q_{\mathrm{pre}}G
\in\operatorname{Mat}_{6\times1785}(\mathbf F_{31}).
$$

No further point selector or $B_R$ condition is missing.

Equivalently, before the already-computed equalizer factors are compressed,
the typed final factorization is

$$
T=L_{\mathrm{residue}}\,
N_{\mathrm{Yoneda/Cech}}\,
\operatorname{EvRes}\,
D_{\mathrm{point}}.
$$

The target equation remains

$$
TH=-3I_6=28I_6\pmod {31}.
$$

No verdict on the six protected classes, Question 11.4, or the Hodge
conjecture is asserted before $Q_{\mathrm{pre}}$ is computed.

The compressed global assembly now proves why that factor cannot be recovered
from the current cache.  The exact source maps have ranks

$$
\operatorname{rank}K_{\mathrm{curve}}=1852,
\qquad
\operatorname{rank}K_{\mathrm{point}}=1785,
\qquad
\operatorname{rank}G=1785.
$$

The six standalone Hodge-sector chains are now exact.  In the frozen
$E_{72}$ endomorphism complex, each

$$
A_{ij}\operatorname{id}_{E_{72}}
$$

is the sum of the 25 matching diagonal $H^2(\mathcal O_X)$ coordinates in
the 1,686-dimensional degree-two basis.  All six are closed and independent
nonboundaries; explicit detectors pair with them as $I_6$.

The first absent package is instead their multiplicative transport into the
actual coupled Cech endomorphism complex.  The true Mayer--Vietoris formal
endpoint $\rho_F$, open endpoint $\rho_U$, common comparison

$$
\Phi_{EF}=\operatorname{Ad}_{\gamma}\rho_U-\rho_F,
$$

and the compatible open cross-channel products have not been emitted.  The
typed endpoint registry fails closed on these rows and contains no zero
placeholders.  This is not a missing selector on an otherwise finished
object: the local bidirectional arrows have not yet been assembled in one
transition-compatible multiplicative model.  The standalone $E_{72}$
chains, curve shadow, correction coefficients, and retained-point equations
still live in different typed targets.

A machine certificate keeps every existing numerical input fixed and realizes
formal completions of every rank from zero through six; the rank-six one has
$TH=28I_6$.  These are countermodels, not geometric outputs.  Therefore the
current artifacts do not determine $Q_{\mathrm{pre}}$, $T$, their ranks, or a
protected-class verdict.  The next constructive event is the common
open/formal multiplicative model for the bidirectional coupling, followed by
the compressed adjoint cochain solve; it is not another point selector or
local residue computation.

## Two valid implementation routes

The preferred route is now the intrinsic derived-generator calculation on
each surface. First construct the point-aware $\mathcal O_Z(4H)$ extension
cone and one common strict representative $h_s$. Then apply derived global
sections and tautological evaluation. Purity explains why the completed
point germs do not choose this cochain on $U=S\setminus W$, even though local
duality determines its cohomology class uniquely.

Choosing that particular strict representative is not a categorical
requirement.  A simultaneous minimal $A_\infty$ model would also be valid,
but it must retain the relevant $m_2$, $m_3$, and higher operations.  The
objectwise Ext groups are insufficient: minimizing the open homotopy moves
its multiplicative information into higher products rather than removing it.

The larger atlas route remains available. It would saturate the translated
generator ideals, compute polynomial-module syzygies, localize them on the
1,296-chart $K$-stable refinement, and recover the same strict extension and
the same later $Q$, $G$, and conductor differential. The 192 edge lifts in
that realization are derived pieces of one global class, not separate
requirements.  The first exact Macaulay probe rules out the smallest
two-generator compression on `Z_2/C0010`: it has two extra simple rational
zeros, and 274 of 297 Hermite border relations are certified nonmembers.
Adding a third translated section removes those two visible points but does
not by itself prove ideal equality.  The full 162-generator translated ideal
now does: degree 11 contains 293 border generators and a fixed degree-12
prefix of 31,751 rows contains the last four, while exact value-and-tangent
jets give the reverse inclusion.  Therefore $K=I_W$ on `Z_2/C0010`.
Hermite-kernel saturation is neither reached nor used, and the certificate
does not promote this one-chart equality to the other 95 charts.

## Primary evidence

- `SIX_HS_LOCAL_SEEDS.md`
- `BIDIRECTIONAL_MC_SOLVER.md`
- `HS_CONDUCTOR_SYNCHRONIZATION.md`
- `GLOBAL_HS_CHART_RESOLUTION.md`
- `HIGHER_THETA_MULTIPLICATION.md`
- `K_EQUIVARIANT_EDGE_AUDIT.md`
- `HS_CHART_LOCAL_SYZYGY_AUDIT.md`
- `IW_FULL_MACAULAY_MEMBERSHIP.md`
- `HS_EXTENSION_FROM_SURFACE_PRESENTATION.md`
- `HS_FACTORED_SURFACE_DESCENT.md`
- `HS_LOCALIZATION_BYPASS_AUDIT.md`
- `PROJECTED_Q_COMPUTABILITY_AUDIT.md`
- `GLOBAL_CHANNEL_COHOMOLOGY.md`
- `SHARED_NORMAL_CONDUCTOR_AUDIT.md`
- `NORMAL_SYZYGY_EQUALIZER.md`
- `HS_POINT_STRATUM_CONTRACT.md`
- `Q_NORMAL_JET_SINGULAR.md`
- `IW_HERMITE_BORDER_BASIS.md`
- `HS_BORDER_TO_LOCAL_FRAMES.md`
- `FORMAL_LOCAL_LCI_EDGE_LIFTS.md`
- `REFINED_HS_FORMAL_COVER.md`
- `PUNCTURED_FORMAL_THETA_COMPARISON.md`
- `FORMAL_HENSEL_JETS.md`
- `THETA_EQUALIZER_FORMAL_COMPOSITION.md`
- `CECH_YONEDA_LIFT_ENGINE.md`
- `REFINED_CECH_YONEDA_COMPOSITION.md`
- `Q_SECTION_RING.md`
- `Q_FIRST_PRINCIPAL_PARTS.md`
- `CURVE_PROTECTED_CONTRACTION_AUDIT.md`
- `Q_SPLIT_OPEN_AUDIT.md`
- `Q_TO_U_SPLIT_FRAME_AUDIT.md`
- `Q_CHAIN_EMBEDDING_AUDIT.md`
- `Q_TO_U_THREE_BRANCH_AUDIT.md`
- `Q_TO_U_OUTER_CECH_CONTRACTION.md`
- `GLOBAL_HS_YONEDA_SOURCE.md`
- `BOUNDARY_ADAPTED_O8_COCHAIN.md`
- `NORMALIZED_FINAL_MATRIX_ASSEMBLY.md`
- `YONEDA_TRANSITION_SCHEMA.md`
- `HS_YONEDA_RESIDUE_AUDIT.md`
- `FORMAL_GLUING_CROSS_RHOM_AUDIT.md`
- `DEGREE2_PAIR_RESIDUAL_AUDIT.md`
- `DERIVED_GENERATOR_CROSS_RHOM_REDUCTION.md`
- `POINT_INTERFACE_PATH_DGA.md`
- `TYPED_PATH_ENDPOINT_REGISTRY.md`
- `E72_H2_IDENTITY_FACE_CHAINS.md`
- `IW_MACAULAY_MEMBERSHIP_PROBE.md`
- `HS_ATLAS_CROSS_RHOM_ADAPTER_CONTRACT.md`
- `results/section12_instantiation/cm_bridge_hs_rank2_k_generator_tree_contraction/tree_contraction_certificate.json`
- `results/section12_instantiation/cm_bridge_hs_rank2_k_generator_tree_contraction_e1/tree_contraction_certificate.json`
- `results/section12_instantiation/cm_bridge_hs_rank2_k_generator_tree_contraction_e2/tree_contraction_certificate.json`
- `results/section12_instantiation/cm_bridge_hs_rank2_k_generator_tree_contraction_e3/tree_contraction_certificate.json`
- `results/section12_instantiation/cm_bridge_hs_rank2_k_coherence_adapter/k_coherence_adapter_certificate.json`

The executable contract is in
`results/section12_instantiation/cm_bridge_yoneda_transition_schema/`.
